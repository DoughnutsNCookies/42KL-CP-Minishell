- Working History												| Done
- System programs												| Done
	- echo with option -n										| Done, need to call the function for it to print
	- cd with a relative or absolute path						| Done, need to call the function for it to print
	- pwd with no options										| Done, need to call the function for it to print
	- export with no options									| Done, need to call the function for it to print, need to expand $ variables before passing into this function
	- unset with no options										| Done, need to call the function for it to print, need to expand $ variables before passing into this function
	- env with no options										| Done, need to call the function for it to print
	- exit with no options										| Done, need to call the function
- Quotation (Expander: Need to expand $ before executing)		| Need to refractor the code and normy
	- ' (Single quotes)											| Done, need more testing
		- Treat it as raw string								| Done, need more testing
	- " (Double quotes)											| Done, need more testing
		- Treat it as raw string except for the $ sign			| Done, need more testing
	- $ (Dollar sign)											| Done, need more testing
	- $? 														| Done, need more testing
		- Exit status of last executed foreground pipeline		| In-progress
		- Correct exit status for all system programs			| In-progress
	- Wildcard * (should work for the current working directory	| In-progress
- Pipex															|
	- > Will overwrite the file (eg. echo "hi" n> test) 		|
	- >> Will append to the file (eg. echo "line2" n>> test)	|
- Signals														|
	- ctrl-C (Print a new prompt on a newline)					| Done
	- ctrl-D (exit the shell)									| Done
	- ctrl-\ (do nothing)										| Done
- Binary Tree													|
	- &&														|
	- ||														|
	- ()														|
- Lexer															| Done, need to integrate from ms_temp into the code