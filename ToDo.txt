- Working History												| Done
- System programs												| Done
	- echo with option -n										| Done
	- cd with a relative or absolute path						| Done
	- pwd with no options										| Done
	- export with no options									| Done
	- unset with no options										| Done
	- env with no options										| Done
	- exit with no options										| Done
- Quotation (Expander: Need to expand $ before executing)		| In-Progress
	- ' (Single quotes)											| Done, need more testing
		- Treat it as raw string								| Done, need more testing
	- " (Double quotes)											| Done, need more testing
		- Treat it as raw string except for the $ sign			| Done, need more testing
	- $ (Dollar sign)											| Done, need more testing
	- $? 														| Done, need more testing
		- Exit status of last executed foreground pipeline		| In-progress
		- Correct exit status for all system programs			| In-progress
	- Wildcard * (should work for the current working directory)| Leaks and Norm
- Pipex															|
	- > Will overwrite the file (eg. echo "hi" > test) 			|
	- >> Will append to the file (eg. echo "line2" >> test)		|
- Executor														| Working Prototype
	- Executes builtin functions								| Done
	- Executes by absolute path	(eg. bin/ls)					|
	- Executes by relative path	(eg. ls)						|
- Signals														|
	- ctrl-C (Print a new prompt on a newline)					| Done
	- ctrl-D (exit the shell)									| Done
	- ctrl-\ (do nothing)										| Done
- Binary Tree													|
	- &&														|
	- ||														|
	- ()														|
- Lexer															| Done, need to integrate from ms_temp into the code