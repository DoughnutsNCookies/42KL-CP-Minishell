- Working History												|
- System programs												| In-progress. CANNOT USE EXECVE. Can check with "whereis" in terminal to check path (eg. whereis ls)
	- echo with option -n										| In-progress
	- cd with a relative or absolute path						| Works for now, need more testing
	- pwd with no options										| In-progress
	- export with no options									| In-progress
	- unset with no options										| In-progress
	- env with no options										| Works for now, need to call the function for it to print
	- exit with no options										| Works for now. need more testing
- Quotation														|
	- ' (Single quotes)											|
		- Treat it as raw string								|
	- " (Double quotes)											|
		- Treat it as raw string except for the $ sign			|
- Pipex															|
	- > Will overwrite the file (eg. echo "hi" n> test) 		|
	- >> Will append to the file (eg. echo "line2" n>> test)	|
- Signals														|
	- ctrl-C (Print a new prompt on a newline)					| Done
	- ctrl-D (exit the shell)									| Done? Exit doesn't print next to $> like in bash
	- ctrl-\ (do nothing)										| Done
- Binary Tree													|
	- &&														|
	- ||														|
	- ()														|
- Wildcard * (should work for the current working directory		|