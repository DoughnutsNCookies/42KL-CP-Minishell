- Working History												| Done
- System programs												| In-progress. CANNOT USE EXECVE. Can check with "whereis" in terminal to check path (eg. whereis ls)
	- echo with option -n										| Done, need to call the function for it to print, need more testing, need to expand $ variables before passing into this function)
	- cd with a relative or absolute path						| Done, need to call the function for it to print
	- pwd with no options										| Done, need to call the function for it to print
	- export with no options									| Done, need to call the function for it to print, need severely more testing, need to expand $ variables before passing into this function)
	- unset with no options										| In-progress
	- env with no options										| Done, need to call the function for it to print
	- exit with no options										| Done, need to call the function for it to print
- Quotation (Expander: Need to expand $ before executing)		|
	- ' (Single quotes)											|
		- Treat it as raw string								|
	- " (Double quotes)											|
		- Treat it as raw string except for the $ sign			|
- Pipex															|
	- > Will overwrite the file (eg. echo "hi" n> test) 		|
	- >> Will append to the file (eg. echo "line2" n>> test)	|
- Signals														|
	- ctrl-C (Print a new prompt on a newline)					| Done
	- ctrl-D (exit the shell)									| Done
	- ctrl-\ (do nothing)										| Done
- Binary Tree													|
	- &&														|
	- ||														|
	- ()														|
- Wildcard * (should work for the current working directory		|